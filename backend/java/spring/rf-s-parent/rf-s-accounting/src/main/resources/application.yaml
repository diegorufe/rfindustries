spring:
  application:
    name: accounting
  datasource:
    driverClassName: org.h2.Driver
    password: sa
    username: sa
    url: jdbc:h2:mem:test
  h2:
    console:
      enabled: true
  sql:
    init:
      mode: 'always'
server:
  port: 4101
  servlet:
    contextPath: /accounting

scheduling:
  syncData: '0 0/30 * * * ?'

